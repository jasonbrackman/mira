python:
  Asset:
    entity:
      actions: []
    task:
      actions: ["start", "publish", "Launch Workarea", "Launch Publish"]
maya:
  Asset:
    entity:
      actions: ["AR", "Launch Folder"]
    task:
      actions: ["QA", "open", "import", "reference", "Launch Workarea", "Launch Publish"]
  Shot:
    entity:
      actions: ["AR", "Launch Folder"]
    task:
      actions: ["QA", "open", "cache", "Launch Cache", "Launch Workarea", "Launch Publish"]